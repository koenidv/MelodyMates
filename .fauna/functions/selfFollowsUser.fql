{
  name: "selfFollowsUser",
  role: null,
  body: Query(
    Lambda(
      ["follows_id"],
      ContainsValue(
        Select("ref", Get(Match(Index("user_id"), Var("follows_id")))),
        Select(
          ["data", "follows"],
          Get(
            Select(
              ["data", "private"],
              Get(Match(Index("user_id"), CurrentIdentity()))
            )
          )
        )
      )
    )
  )
}